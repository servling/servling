// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/servling/servling/ent/domain"
)

// Domain is the model entity for the Domain schema.
type Domain struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Certificate holds the value of the "certificate" field.
	Certificate *string `json:"certificate,omitempty"`
	// Key holds the value of the "key" field.
	Key *string `json:"key,omitempty"`
	// CloudflareEmail holds the value of the "cloudflare_email" field.
	CloudflareEmail *string `json:"cloudflare_email,omitempty"`
	// CloudflareAPIKey holds the value of the "cloudflare_api_key" field.
	CloudflareAPIKey *string `json:"cloudflare_api_key,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DomainQuery when eager-loading is set.
	Edges        DomainEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DomainEdges holds the relations/edges for other nodes in the graph.
type DomainEdges struct {
	// Ingresses holds the value of the ingresses edge.
	Ingresses []*Ingress `json:"ingresses,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// IngressesOrErr returns the Ingresses value or an error if the edge
// was not loaded in eager-loading.
func (e DomainEdges) IngressesOrErr() ([]*Ingress, error) {
	if e.loadedTypes[0] {
		return e.Ingresses, nil
	}
	return nil, &NotLoadedError{edge: "ingresses"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Domain) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case domain.FieldID, domain.FieldName, domain.FieldCertificate, domain.FieldKey, domain.FieldCloudflareEmail, domain.FieldCloudflareAPIKey:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Domain fields.
func (d *Domain) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case domain.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				d.ID = value.String
			}
		case domain.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				d.Name = value.String
			}
		case domain.FieldCertificate:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field certificate", values[i])
			} else if value.Valid {
				d.Certificate = new(string)
				*d.Certificate = value.String
			}
		case domain.FieldKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field key", values[i])
			} else if value.Valid {
				d.Key = new(string)
				*d.Key = value.String
			}
		case domain.FieldCloudflareEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field cloudflare_email", values[i])
			} else if value.Valid {
				d.CloudflareEmail = new(string)
				*d.CloudflareEmail = value.String
			}
		case domain.FieldCloudflareAPIKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field cloudflare_api_key", values[i])
			} else if value.Valid {
				d.CloudflareAPIKey = new(string)
				*d.CloudflareAPIKey = value.String
			}
		default:
			d.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Domain.
// This includes values selected through modifiers, order, etc.
func (d *Domain) Value(name string) (ent.Value, error) {
	return d.selectValues.Get(name)
}

// QueryIngresses queries the "ingresses" edge of the Domain entity.
func (d *Domain) QueryIngresses() *IngressQuery {
	return NewDomainClient(d.config).QueryIngresses(d)
}

// Update returns a builder for updating this Domain.
// Note that you need to call Domain.Unwrap() before calling this method if this Domain
// was returned from a transaction, and the transaction was committed or rolled back.
func (d *Domain) Update() *DomainUpdateOne {
	return NewDomainClient(d.config).UpdateOne(d)
}

// Unwrap unwraps the Domain entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (d *Domain) Unwrap() *Domain {
	_tx, ok := d.config.driver.(*txDriver)
	if !ok {
		panic("ent: Domain is not a transactional entity")
	}
	d.config.driver = _tx.drv
	return d
}

// String implements the fmt.Stringer.
func (d *Domain) String() string {
	var builder strings.Builder
	builder.WriteString("Domain(")
	builder.WriteString(fmt.Sprintf("id=%v, ", d.ID))
	builder.WriteString("name=")
	builder.WriteString(d.Name)
	builder.WriteString(", ")
	if v := d.Certificate; v != nil {
		builder.WriteString("certificate=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	if v := d.Key; v != nil {
		builder.WriteString("key=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	if v := d.CloudflareEmail; v != nil {
		builder.WriteString("cloudflare_email=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	if v := d.CloudflareAPIKey; v != nil {
		builder.WriteString("cloudflare_api_key=")
		builder.WriteString(*v)
	}
	builder.WriteByte(')')
	return builder.String()
}

// Domains is a parsable slice of Domain.
type Domains []*Domain
